# Core (survival + classification pipelines)
numpy>=1.21,<3
pandas>=1.3,<3
scipy>=1.7
scikit-learn>=1.0
matplotlib>=3.5
lifelines>=0.27
scikit-survival>=0.19.0

# Required for the primary classification benchmark (Trimodal XGBoost, ensembles)
xgboost>=1.5

# Optional — enables additional models and plots
# lightgbm>=3.3      # LightGBM in extended classification benchmark
# shap>=0.41         # SHAP plots in extras_consolidated.py and classification_consolidated.py

# Optional DeepSurv - large install (~1 GB), needed to reproduce DeepSurv C-index results
# pip install torch>=2.0 torchtuples>=0.2.0 pycox>=0.2.3
# torch>=2.0
# torchtuples>=0.2.0
# pycox>=0.2.3
